server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:11306/textexpander?useSSL=false&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: dev
    password: dev
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.MySQL5Dialect
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  application:
    name: test
logging:
  level:
    org:
      springframework.web: INFO

collablynk:
  service-id: prior-auth-intelligent-router
  client-id: prior-auth-intelligent-router-service
  accounts-service-base-url: "https://auth.stage.kollab.link"
  client-secret: "B3DY1fFiUZZOLzZponCoiZdPjOouy8tu"
